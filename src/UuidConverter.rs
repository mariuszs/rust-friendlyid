
struct UuidConverter {
}

impl UuidConverter {

    // fn  to_big_integer( uuid: &UUID) -> BigInteger  {
    //     return BigIntegerPairing::pair(&BigInteger::value_of(&uuid.get_most_significant_bits()), &BigInteger::value_of(&uuid.get_least_significant_bits()));
    // }
    //
    // fn  to_uuid( value: &BigInteger) -> UUID  {
    //     let unpaired: Vec<BigInteger> = BigIntegerPairingrust::unpair(&value);
    //     return UUID::new(&unpaired[0].long_value_exact(), &unpaired[1].long_value_exact());
    // }
}



